export enum AccumulatorExpressionType {
	$addToSet = '$addToSet',
	$avg = '$avg',
	$count = '$count',
	$first = '$first',
	$last = '$last',
	$max = '$max',
	$mergeObjects = '$mergeObjects',
	$min = '$min',
	$push = '$push',
	$sum = '$sum',
}
